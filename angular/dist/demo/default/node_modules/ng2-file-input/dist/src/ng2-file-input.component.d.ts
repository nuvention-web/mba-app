import { FileInputHandlerService } from './file-input-handler.service';
import { EventEmitter, OnInit, OnDestroy } from '@angular/core';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
import { Ng2FileInputOptions } from './ng2-file-input-options.class';
export declare class Ng2FileInputComponent implements OnInit, OnDestroy {
    private fileInputHandlerService;
    private sanitizer;
    private defaultOptions;
    private alreadyEmitted;
    private options;
    fileIsOver: boolean;
    invalidFile: boolean;
    id: string;
    dropText: string;
    browseText: string;
    removeText: string;
    invalidFileText: string;
    invalidFileTimeout: number;
    multiple: boolean;
    accept: string;
    removable: boolean;
    showPreviews: boolean;
    extensions: string[];
    outputAction: EventEmitter<{}>;
    outputRemoved: EventEmitter<{}>;
    outputAdded: EventEmitter<{}>;
    outputInvalidDenied: EventEmitter<{}>;
    outputCouldNotRemove: EventEmitter<{}>;
    outputCouldNotAdd: EventEmitter<{}>;
    constructor(fileInputHandlerService: FileInputHandlerService, sanitizer: DomSanitizer, defaultOptions: Ng2FileInputOptions);
    ngOnInit(): void;
    ngOnDestroy(): void;
    fileOver(fileIsOver: boolean): void;
    onFileDrop(file: File): void;
    fileSelected(event: any): void;
    getCurrentFiles(): File[];
    removeFile(file: File): void;
    getObjectUrl(file: File): SafeResourceUrl;
    private handleFile(file);
    private emitOutput(file, action);
    private isValidFile(file);
    private generateId();
}
